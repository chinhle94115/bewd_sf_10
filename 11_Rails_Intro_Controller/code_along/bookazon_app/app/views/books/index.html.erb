<div class = "col-12">
  <h1 style="color:#cc3333;">Our Book Collection</h1>

  <div class = "well">
    <% @books.each do |book| %>
    <ul> <li><%= book.title %> </ul>
    <% end %>
  </div>
</div>

<!--
<% initiates ruby code?
#if <%= , makes it viewable, whereas <% is action item, but not viewable -->
